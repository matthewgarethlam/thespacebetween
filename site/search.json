{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"The Space Between","text":"<p>Welcome to my personal website for keeping and sharing notes, learnings and tips I've picked up along the way. </p> <p>This is a collection of explainers, tutorials, quick notes (mostly for my brain to understand) and code snippets on concepts I've found useful to formalise. </p>","path":["The Space Between"],"tags":[]},{"location":"Coding/A%20crash%20course%20in%20Git/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Coding","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/","level":1,"title":"Stats shouldn't be scary!","text":"<p>Some text here </p>","path":["Statistics","Stats shouldn't be scary!"],"tags":[]},{"location":"Statistics/Bayesian/A%20crash%20course%20in%20Bayesian%20statistics/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Bayesian","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Bayesian/Bayesian%20sequential%20updating/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Bayesian","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Causal/Difference%20in%20differences/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Causal","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Causal/Synthetic%20control%20analysis/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Causal","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Fundamentals/Hypothesis%20testing/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Fundamentals","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Fundamentals/Regression/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Fundamentals","Git - I accidentally got into vi when I committed"],"tags":[]},{"location":"Statistics/Fundamentals/Significance/","level":1,"title":"Git - I accidentally got into vi when I committed","text":"<p>It's generally easier to use the <code>-m</code> flag when doing a git commit, ie:</p> <pre><code>&gt; git commit -m \"Description of changes\"\n</code></pre> <p>but when you just type <code>git commit</code> you end up in vi (if that's your default text editor).</p> <p>The way you finish off your commit is to:</p> <ol> <li>Type in your commit message</li> <li>Hit \"Esc\" to enter command mode (\"i\" to gets back to insert mode if you need to write some more)</li> <li>Type <code>:wq</code> for 'write' and 'quit'</li> </ol> <p>and that's it.</p> <p>If you want to quit without saving it's <code>:q!</code></p>","path":["Statistics","Fundamentals","Git - I accidentally got into vi when I committed"],"tags":[]}]}